spring:
 application:
  name: config-service
 cloud:
  config:
   server:
    git:
     uri: http://github.com/mateoQuotteri/config-server
     default-label: main
     clone-on-start: true
     force-pull: true

server:
 port: 8888

eureka:
 instance:
  hostname: localhost

